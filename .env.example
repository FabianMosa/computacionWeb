# URL pública del sitio (Open Graph / metadata)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Webhook opcional: POST JSON con el lead (CRM, Zapier, Make, etc.)
CONTACT_WEBHOOK_URL=

# Solo en despliegue HTTPS: envía cabecera Strict-Transport-Security (no usar en http://localhost)
ENABLE_HSTS=
